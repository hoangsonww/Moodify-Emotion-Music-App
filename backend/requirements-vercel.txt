# Moodify Django API -- slim dependency set for the Vercel deployment.
#
# Deliberately excludes ALL machine-learning packages (torch, tensorflow,
# transformers, librosa, opencv, moviepy, fer, facenet-pytorch, spotipy).
# Inference now lives in the Modal service (modal_inference/); the Django
# tier only proxies to it. Keeping ML out is what makes the Vercel bundle
# fit under the size limit and keeps cold starts fast.
#
# For the Vercel build this file must be the `requirements.txt` adjacent to
# vercel_wsgi.py -- see vercel_wsgi.py docstring.

# --- Web framework ---
Django==5.1.1
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-rest-framework-mongoengine==3.4.1
django-cors-headers==4.4.0
drf-yasg==1.21.7

# --- MongoDB (now the only datastore -- SQLite is dropped) ---
mongoengine==0.29.1
pymongo==4.9.1
dnspython==2.6.1

# --- Auth + outbound HTTP to the Modal inference service ---
PyJWT==2.9.0
requests==2.32.3

# --- Config / static ---
python-decouple==3.8
python-dotenv==1.0.1
whitenoise==6.7.0

# NOTE (plan §5.3): django.contrib.admin/auth/sessions and dj-rest-auth /
# allauth are removed -- they require a SQL database. Auth is JWT-only,
# backed by a mongoengine User document (users/documents.py).

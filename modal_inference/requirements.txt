# Moodify Modal inference service -- CPU dependency set.
#
# PyTorch CPU wheels are installed separately in modal_app.py via
#   .pip_install(..., index_url="https://download.pytorch.org/whl/cpu")
# to keep the container image small. Do NOT add torch/torchvision/
# torchaudio here.
#
# For the NVIDIA GPU variant see requirements-gpu.txt.

# --- Web layer (Modal provides the ASGI server) ---
fastapi==0.115.0
python-multipart==0.0.12
pydantic==2.9.2

# --- Text emotion (BERT / Transformers) ---
transformers==4.44.2
tokenizers==0.19.1
safetensors==0.4.5
huggingface-hub==0.25.1

# --- Speech emotion (librosa + scikit-learn) ---
numpy==1.26.4
scikit-learn==1.5.2
librosa==0.10.2.post1
soundfile==0.12.1
audioread==3.0.1

# --- Facial emotion (FER bundled detector) ---
# FER pulls in opencv-contrib-python + mtcnn; it runs on TensorFlow, which
# is pinned to the legacy-Keras stack so the bundled .h5 model loads
# cleanly (TF_USE_LEGACY_KERAS=1 is set on the image in modal_app.py).
fer==22.5.1
tensorflow-cpu==2.17.0
tf-keras==2.17.0
mtcnn==0.1.1

# --- Auth + outbound HTTP ---
PyJWT==2.9.0
requests==2.32.3

# --- Model weight download (used by download_models.py) ---
gdown==5.2.0

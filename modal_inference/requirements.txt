# Moodify Modal inference service -- CPU dependency set.
#
# PyTorch CPU wheels (torch + torchvision, the latter required by
# facenet-pytorch) are installed separately in modal_app.py via
#   .pip_install(..., index_url="https://download.pytorch.org/whl/cpu")
# to keep the container image small. Do NOT add torch/torchvision here.
#
# For the NVIDIA GPU variant see requirements-gpu.txt.

# --- Web layer (Modal provides the ASGI server) ---
fastapi==0.115.0
python-multipart==0.0.12
pydantic==2.9.2

# --- Text emotion (BERT / Transformers) ---
transformers==4.44.2
tokenizers==0.19.1
safetensors==0.4.5
huggingface-hub==0.25.1

# --- Speech emotion (librosa + scikit-learn) ---
numpy==1.26.4
scikit-learn==1.5.2
librosa==0.10.2.post1
soundfile==0.12.1
audioread==3.0.1

# --- Facial emotion (FER detector) ---
# FER uses facenet-pytorch's MTCNN for face detection and runs its emotion
# CNN on Keras/TensorFlow. tf-keras + TF_USE_LEGACY_KERAS=1 (set on the
# image in modal_app.py) keep FER's bundled .h5 model loadable under TF 2.17.
# FER also transitively pulls opencv-contrib-python, moviepy, Pillow, etc.
fer==22.5.1
facenet-pytorch==2.6.0
tensorflow-cpu==2.17.0
tf-keras==2.17.0

# --- Auth + outbound HTTP ---
PyJWT==2.9.0
requests==2.32.3

# --- Model weight download (used by download_models.py) ---
gdown==5.2.0

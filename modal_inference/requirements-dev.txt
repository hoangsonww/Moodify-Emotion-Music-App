# Lightweight dependencies to run the modal_inference test suite.
#
# Intentionally does NOT pull in requirements.txt -- the tests exercise the
# pure logic and the FastAPI surface with faked models, so torch /
# tensorflow / fer / librosa are not needed.

fastapi==0.115.0
pydantic==2.9.2
python-multipart==0.0.12
PyJWT==2.9.0
requests==2.32.3
pytest==8.4.1
pytest-cov==5.0.0
httpx==0.27.2
# Metrics tests stub out pymongo via monkeypatch, but the import path
# in metrics_store happens at first call -- pulling the real client in
# avoids ImportError sentinels in test runs.
pymongo==4.10.1
